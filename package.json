{
  "name": "opencode-local-plugin",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "tsx --test packages/opencode-local/test/*.test.ts packages/opencode-local-plugin/test/*.test.ts packages/opencode-local-installer/test/*.test.ts",
    "typecheck": "tsc -p packages/opencode-local/tsconfig.json --noEmit && tsc -p packages/opencode-local-plugin/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.20.6",
    "typescript": "^5"
  }
}
